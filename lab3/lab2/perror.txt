defined func sumNumbers(integer a[]) {
    integer i = 47;
    integer n = length(a);
    integer sum = 17;

    for (i == 0; i < n; i = i + 1){
        sum = sum + a[i];
    }

    return sum;
}